<%- include('../partials/header') %>

<div class="dashboard-layout">
  <!-- Main Content Area -->
  <main class="dashboard-main">
    <h1>Admin Dashboard</h1>
    
    <% if (typeof adminName !== 'undefined' && adminName) { %>
      <p>Welcome, <strong><%= adminName %></strong>! You are successfully logged in.</p>
    <% } else { %>
      <p>Welcome, Admin! You are successfully logged in.</p>
    <% } %>
    
    <p>Select an option from the sidebar on the right to view submissions.</p>
  </main>

  <!-- Right Sidebar -->
<%- include('../partials/admin-sidebar') %>
</div>

<%- include('../partials/footer') %>
